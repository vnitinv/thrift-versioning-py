#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from interfaces import InterfacesService
from interfaces.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  ReturnStatus V4InterfaceAdd(string if_name, string prefix, i32 unit, i32 size)'
  print '  ReturnStatus V4InterfaceDelete(string if_name, i32 unit, string prefix, i32 prefix_len)'
  print '   InterfaceGet(string if_name)'
  print '  IfStatus InterfaceStatsGet(string if_name)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = InterfacesService.Client(protocol)
transport.open()

if cmd == 'V4InterfaceAdd':
  if len(args) != 4:
    print 'V4InterfaceAdd requires 4 args'
    sys.exit(1)
  pp.pprint(client.V4InterfaceAdd(args[0],args[1],eval(args[2]),eval(args[3]),))

elif cmd == 'V4InterfaceDelete':
  if len(args) != 4:
    print 'V4InterfaceDelete requires 4 args'
    sys.exit(1)
  pp.pprint(client.V4InterfaceDelete(args[0],eval(args[1]),args[2],eval(args[3]),))

elif cmd == 'InterfaceGet':
  if len(args) != 1:
    print 'InterfaceGet requires 1 args'
    sys.exit(1)
  pp.pprint(client.InterfaceGet(args[0],))

elif cmd == 'InterfaceStatsGet':
  if len(args) != 1:
    print 'InterfaceStatsGet requires 1 args'
    sys.exit(1)
  pp.pprint(client.InterfaceStatsGet(args[0],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
